import HowItWorksClient from "./HowItWorksClient";

export const metadata = {
  title: "How SasaKazi Works | From Business Need to Successful Delivery",
  description: "See how SasaKazi defines client needs, assesses tech talent, creates the right match and supports successful delivery.",
};

export default function HowItWorksPage() {
  return <HowItWorksClient />;
}
